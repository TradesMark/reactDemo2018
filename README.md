Используем прикольный инструмент `portfinder` для поиска свободного порта и делаем запуск менее зависимым от хардкодинга констант

работает переменная среды:
```bash
PORT=1234 yarn start
```
работает найденное значение:
```bash
yarn start
```
